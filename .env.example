# Database Configuration
# Railway will automatically provide DATABASE_URL when you add PostgreSQL
DATABASE_URL=postgresql://user:password@host:port/database

# Optional: Set to production for deployment
FLASK_ENV=production

# Optional: Secret key for Flask sessions
SECRET_KEY=your-secret-key-here

# Note: OPENAI_API_KEY is provided by users through the UI, not stored in environment
